= search_form_for @q, url: profiles_path do |f|
  = f.label :email_cont
  = f.search_field :email_cont
  = f.submit

= link_to 'Clear', profiles_path, class: 'btn btn-primary'

- @users.each do |user|
  %h4= user.email
