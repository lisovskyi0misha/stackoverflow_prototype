.header
  .nav-item
    = link_to 'See all questions', questions_path, class: 'header-button'
  .nav-item
    = link_to 'Global search', search_path, class: 'header-button'
  .nav-item
    - if current_user
      = link_to 'Log out', destroy_user_session_path, method: :delete, class: 'header-button'
    - else
      = link_to 'Sign in', new_user_session_path, class: 'header-button'
